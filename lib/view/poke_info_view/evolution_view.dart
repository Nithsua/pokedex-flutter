import 'package:flutter/material.dart';

class EvolutionView extends StatelessWidget {
  const EvolutionView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [],
    );
  }
}
