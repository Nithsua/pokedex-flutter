import 'package:flutter/material.dart';

class BaseStatView extends StatelessWidget {
  const BaseStatView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [],
    );
  }
}
